# VerilogDB Code Completion

This domain contains focused RTL code-completion tasks derived from VerilogDB
designs. Each prompt removes a meaningful 10-20 line section and asks the model
to return the completed Verilog module. Testbenches target the missing section
rather than the full original system.
